const todoList=[];

function addWork(){
  const i=document.querySelector('.js-work-input');
  const namework=inputElement.value;

  todoList.push(nameWork);
  console.log(nameWork);

  inputWork.value='';
}

