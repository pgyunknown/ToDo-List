const todoList=[];

function addWork(){
  let inputElement=document.querySelector('.js-work-input');=inputEle.value;
  const

  todoList.push(nameWork);
  console.log(nameWork);

  inputWork.value='';
}

