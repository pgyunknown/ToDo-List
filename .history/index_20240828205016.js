const todoList=[];

function addWork(){
  const inputElement=document.querySelector('.js-work-input');=inputEle.value;
  const

  todoList.push(nameWork);
  console.log(nameWork);

  inputWork.value='';
}

